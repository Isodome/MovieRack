<Window x:Class="WinMovieRack.GUI.BigPicture"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BigPicture" Height="300" Width="300" ResizeMode="NoResize" SizeToContent="Height" Topmost="True" WindowStyle="None" WindowStartupLocation="Manual" MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Grid>
        <Image Height="auto" HorizontalAlignment="Stretch" Name="bigPicture" VerticalAlignment="Stretch" Width="auto" MouseLeave="bigPicture_MouseLeave" MouseEnter="bigPicture_MouseEnter" />
        <Grid Name="buttonGrid" Height="auto" VerticalAlignment="Bottom" Opacity="0" Margin="70,0,67,0" HorizontalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="118" />
                <ColumnDefinition Width="23" />
            </Grid.ColumnDefinitions>
            <Button Click="quitButton_Click" Content="Quit" Height="auto" Margin="80,0,0,0" Name="quitButton" Opacity="1" VerticalAlignment="Bottom" MouseEnter="quitButton_MouseEnter" MouseLeave="quitButton_MouseLeave" Width="38" HorizontalAlignment="Center" />
            <Button Content="Save" Height="auto" Name="saveButton" MouseEnter="saveButton_MouseEnter" MouseLeave="saveButton_MouseLeave" Margin="26,0,43,0" VerticalAlignment="Bottom" Width="49" HorizontalAlignment="Center" />
        </Grid>
    </Grid>
</Window>
